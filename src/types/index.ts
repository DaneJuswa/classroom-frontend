export type Subject = {
    id: number;
    name: string;
    code: string;
    department: string;
    description: string;
    createdAt: Date;
}

export type Users = {
    id: number;
    name: string;
    age: number;
}